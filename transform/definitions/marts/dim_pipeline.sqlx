config {
  type: "table",
  schema: "dm_rework_crm",
  description: "Dimension: sales pipelines",
  assertions: {
    uniqueKey: ["pipeline_id"],
    nonNull: ["pipeline_id", "pipeline_name"]
  }
}

SELECT
  pipeline_id,
  pipeline_name,
  pipeline_content
FROM ${ref("stg_pipeline")}
